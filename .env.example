# ── Copy this file to .env and fill in your values ──
# cp .env.example .env

# ── PostgreSQL ──
DB_USER=cpanel
DB_PASSWORD=change_me_strong_password
DB_NAME=cpanel

# ── JWT ──
JWT_SECRET=change_me_very_long_random_secret_at_least_64_chars
JWT_EXPIRES_IN=7d

# ── API (internal — nginx proxies /api/* to this port) ──
PORT=3001

# ── Google Maps (Vite frontend) ──
VITE_GOOGLE_MAPS_KEY=AIzaSyChT-oYZEjTshF5uyBGsy_d9Kq2bd4d4HQ
